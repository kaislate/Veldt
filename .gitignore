*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
key.properties
*.apk
/docs

# SDD review artifacts (also self-ignored under .superpowers/sdd/)
/.superpowers/

# Agent worktrees (a full nested copy of the repo — never commit)
/.claude/

# Kotlin daemon session artifacts
/.kotlin/
